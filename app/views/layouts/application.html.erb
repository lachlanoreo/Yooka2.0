<!DOCTYPE html>
<html class="h-full">
  <head>
    <title><%= content_for(:title) || "Yooka - GTD Plan" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full bg-zinc-50 text-zinc-900 antialiased">
    <div class="flex h-full flex-col">
      <nav class="border-b border-zinc-200 bg-white">
        <div class="mx-auto flex h-14 items-center justify-between px-4">
          <div class="flex items-center gap-6">
            <a href="<%= root_path %>" class="text-lg font-semibold text-zinc-900">Yooka</a>
            <div class="flex items-center gap-1">
              <%= link_to "Plan", plan_path, class: "rounded-md px-3 py-2 text-sm font-medium #{request.path == plan_path ? 'bg-zinc-100 text-zinc-900' : 'text-zinc-600 hover:bg-zinc-50 hover:text-zinc-900'}" %>
            </div>
          </div>
          <div class="flex items-center gap-2">
            <span class="text-sm text-zinc-500"><%= Date.current.strftime("%A, %B %-d") %></span>
          </div>
        </div>
      </nav>

      <main class="flex-1 overflow-hidden">
        <%= yield %>
      </main>
    </div>
  </body>
</html>

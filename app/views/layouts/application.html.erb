<!DOCTYPE html>
<html class="h-full">
  <head>
    <title><%= content_for(:title) || "Yooka - GTD Plan" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="yooka-container h-full">
    <div class="flex h-full flex-col">
      <!-- Navigation -->
      <nav class="yooka-nav">
        <div class="mx-auto flex h-16 items-center justify-between px-6">
          <div class="flex items-center gap-8">
            <a href="<%= root_path %>" class="yooka-nav-brand">Yooka</a>
            <div class="flex items-center gap-1">
              <%= link_to "Plan", plan_path, class: "yooka-nav-link #{request.path == plan_path ? 'active' : ''}" %>
            </div>
          </div>
          <div class="flex items-center gap-3">
            <span class="yooka-date"><%= Date.current.strftime("%A, %B %-d") %></span>
          </div>
        </div>
      </nav>

      <!-- Main Content -->
      <main class="flex-1 overflow-hidden">
        <%= yield %>
      </main>
    </div>
  </body>
</html>
